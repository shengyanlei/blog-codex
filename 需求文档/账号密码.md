## Codex Blog 开发环境账号/密码与 IP 说明

> 以下信息仅适用于本地/内网开发测试，请勿直接用于生产环境。上线前需通过配置中心或环境变量重新下发正式凭据，并开启最小权限控制。

### 1. 数据库

| 系统       | 地址（IP:端口）           | 数据库名    | 账号 | 密码 | 备注                     |
| ---------- | ------------------------- | ----------- | ---- | ---- | ------------------------ |
| MySQL 8.0  | 127.0.0.1:3306            | codex_auth  | root | abc123! | `auth-service` 默认配置  |
| MySQL 8.0  | 127.0.0.1:3306            | codex_blog  | root | abc123! | 其他服务共享，可拆分     |

### 2. 缓存 & 消息

| 组件     | 地址（IP:端口） | 账号 | 密码 | 备注                               |
| -------- | --------------- | ---- | ---- | ---------------------------------- |
| Redis 7  | 127.0.0.1:6379  | 无   | 无   | 本地未设置密码，生产必须开启权限   |
| RabbitMQ | 127.0.0.1:5672  | guest| guest| 如未启用可忽略，建议单独账户       |

### 3. 管理后台 / API 账号

| 系统                | 登录地址/IP        | 账号 (email)        | 密码        | 说明                               |
| ------------------- | ------------------ | ------------------- | ----------- | ---------------------------------- |
| 管理端 Demo (admin-ui) | 本地静态页 `index.html` | admin@example.com   | Admin12345! | 调用 Auth API，启动 Auth Service 后可登录 |
| Swagger/OpenAPI     | 对应服务 `http://127.0.0.1:{port}` | - | - | 使用 JWT Token 访问，需要先登录获取 |

### 4. JWT / 安全配置

- `auth.jwt.secret`：`changemechangemechangemechangeme`（位于 `auth-service/application.yml`），仅供本地使用，部署时务必更换。
- 访问 API 时需在请求头携带 `Authorization: Bearer <token>`，token 可通过管理员账号登录获得。

### 5. 启动节点 IP 参考

| 服务/模块          | 默认启动 IP | 端口 | 备注                              |
| ------------------ | ----------- | ---- | --------------------------------- |
| auth-service       | 127.0.0.1   | 8081 | `mvn -pl auth-service spring-boot:run` |
| content-service    | 127.0.0.1   | 8082 | 需配置数据库/Redis                |
| gateway (BFF)      | 127.0.0.1   | 8080 | 对外统一入口                      |
| admin-ui (静态)    | 127.0.0.1   | 4173 | 例如 `npx serve backend/admin-ui` |

> 若在云服务器或容器中运行，请将上述 IP 替换为实际内网/外网地址，并在安全组中限制访问范围。
